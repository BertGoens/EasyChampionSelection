Make YT Tutorial

Saving the client image works but somehow my 'bad' image overwrites it as last save.
Everything else should work correctly

Find out how LSI creates its own categories and the game accepts them?!

Modify TaskbarIcon (dll?) so it supports multiple popups

For Distribution:
? Save in /Program Files/ (uiAccess=True > Must be saved in this folder)
Save as /Program Files/Easy Champion Selection/*.exe